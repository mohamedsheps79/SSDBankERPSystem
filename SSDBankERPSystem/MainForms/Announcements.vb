Public Class Announcements

    Private Sub Announcements_Load(sender As Object, e As EventArgs) Handles MyBase.Load
        'str = " select Investigations.EmployeeID,EmployeeName from Investigations,Employees WHERE " & _
        '             " Employees.EmployeeID = Investigations.EmployeeID And AnouncementStatus = 0 "
        'GetData(0)
        'If ds.Tables.Count > 0 Then
        '    If ds.Tables(0).Rows.Count > 0 Then

        '    End If
        'End If
    End Sub
End Class